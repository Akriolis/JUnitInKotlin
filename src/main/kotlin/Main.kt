// unit testing is used to test individual units of source code,
// such as classes or functions

class Totaller (var total: Int = 0){
    fun add(num: Int): Int{
        total += num
        return total
    }
}

fun main(){

}